# To execute test: "python test_1/test.py"
from typing import Optional 

# Don't change this function
class ListNode:
    def __init__(self, val=0, next=None):
        self.val = val
        self.next = next

# Solution to be completed
def solution (list1: Optional[ListNode], list2: Optional[ListNode]) -> Optional[ListNode]:
    """
        Args: (list1: head node of the first singly linked list, list2: head node of the second singly linked list)
        return: head node of the sorted list
    """
    
    return ListNode(1, ListNode(1, ListNode(2, ListNode(3, ListNode(4, ListNode(4))))))




      

